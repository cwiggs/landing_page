<?php

	// IIS won't work with this, but apache does
	$root = $_SERVER['DOCUMENT_ROOT']

	// I couldn't get this to work, but it's supposed to work with ISS and Apache
	//define( 'ROOT_DIR', dirname(__FILE__) );

?>
